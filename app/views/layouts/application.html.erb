<!DOCTYPE html>

<html>
  <head>
    <title>Leftover Recipe Generator</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>
    <nav>
      <div>
        <a class="navbar-brand" href="/">Home</a>
        <% if session.fetch(:user_id) == nil %>
            <a class="navbar-brand" href="/user_sign_up">Sign Up</a>
            <a class="navbar-brand" href="/user_sign_in">Sign In</a>
        <% else %>
            <a class="navbar-brand" href="/user_sign_out">Sign Out</a>
            <a class="navbar-brand" href="/bookmarks">My bookmarks</a>
        <% end %>
      </div>
    </nav>

    <% if notice.present?%>
      <div style="border: thin green solid; color: green">
        <%= notice %>
      </div>
    <% end %>

    <% if alert.present?%>
      <div style="border: thin red solid; color: red">
        <%= alert %>
      </div>
    <% end %>
    
    <%= yield %>
  </body>
</html>
